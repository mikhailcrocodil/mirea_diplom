import styled from "styled-components";
import { APP_COLORS } from "../../../shared/utils/colors/colors.constants";

export const AppContainer = styled("div")`
  background: ${APP_COLORS.background};
  display: flex;
  flex-direction: column;
`;
